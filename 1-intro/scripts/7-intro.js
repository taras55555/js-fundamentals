const login = window.prompt("Enter yout username");
const email = window.prompt("Enter yout email");
const password = window.prompt("Enter yout password");

console.log("Dear " + login + ", your email is " + email + ", your password is " + password);